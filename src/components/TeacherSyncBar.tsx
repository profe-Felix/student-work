import React from 'react';

// Temporary compile-safe stub. Accepts any props from parent components.
type Props = any;

export default function TeacherSyncBar(_props: Props) {
  // TODO: restore UI + realtime wiring after build is green.
  return null;
}
